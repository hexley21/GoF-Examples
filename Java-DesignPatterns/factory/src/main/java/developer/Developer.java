package developer;

/**
 * The Developer interface defines methods for writing code, writing tests, and pushing changes to a repository.
 */
public interface Developer {
    void writeCode();
    void writeTests();
    void pushChanges();
}
